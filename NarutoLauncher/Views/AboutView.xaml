<UserControl x:Class="NarutoLauncher.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <UserControl.Resources>
        <!-- 与设置页一致 -->
        <Style x:Key="SettingRowStyle" TargetType="Border">
            <Setter Property="Padding" Value="20,14"/>
            <Setter Property="Background" Value="{DynamicResource CardBackgroundFillColorDefaultBrush}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Margin" Value="0,4"/>
        </Style>
        <Style x:Key="SettingIconStyle" TargetType="Border">
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Margin" Value="0,0,14,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="SettingIconTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe Fluent Icons"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Foreground" Value="{DynamicResource AccentTextFillColorPrimaryBrush}"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="ItemNameStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
        </Style>
        <Style x:Key="ItemDescStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}"/>
            <Setter Property="Margin" Value="0,2,0,0"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
        <Style x:Key="GroupTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}"/>
            <Setter Property="Margin" Value="4,10,0,4"/>
        </Style>
        <Style x:Key="PageTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer>
        <StackPanel Margin="28,20,28,24">

            <TextBlock Text="关于" Style="{StaticResource PageTitleStyle}"/>

            <!-- ===== 版本信息 ===== -->
            <Border Style="{StaticResource SettingRowStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Width="34" Height="34" CornerRadius="8"
                            Background="{DynamicResource ControlFillColorSecondaryBrush}"
                            VerticalAlignment="Center" Margin="0,0,12,0">
                        <Image Source="/assets/favicon.png" Stretch="Uniform"
                               RenderOptions.BitmapScalingMode="HighQuality"/>
                    </Border>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="火影忍者Online 启动器" Style="{StaticResource ItemNameStyle}"/>
                        <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                            <TextBlock Text="作者 XiaoWu · 基于 " Style="{StaticResource ItemDescStyle}"/>
                            <TextBlock Text="CEFFlashGameHost" Style="{StaticResource ItemDescStyle}"/>
                            <TextBlock Text=" 改造" Style="{StaticResource ItemDescStyle}"/>
                        </StackPanel>
                    </StackPanel>
                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Background="{DynamicResource AccentFillColorDefaultBrush}"
                                CornerRadius="9" Padding="9,2" VerticalAlignment="Center">
                            <TextBlock Text="v1.0.0" FontSize="11" FontWeight="SemiBold" Foreground="White"/>
                        </Border>
                        <ui:Button x:Name="UpdateButton" Content="检查更新" Appearance="Primary"
                                   Click="OnCheckUpdate" Margin="12,0,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- ===== 关于 ===== -->
            <TextBlock Text="关于这个启动器" Style="{StaticResource GroupTitleStyle}"/>
            <Border Style="{StaticResource SettingRowStyle}">
                <StackPanel>
                    <TextBlock Style="{StaticResource ItemDescStyle}" FontSize="13" LineHeight="22"
                               Text="基于 CEF 87 内核开发。从 2020 年的想法到如今的落地，期间给 Flash 插件打补丁、修各种崩溃，近乎每天 16 小时的高强度开发。愿这份优化能取代掉满是广告的游戏大厅。"/>
                </StackPanel>
            </Border>

            <!-- ===== 致谢 ===== -->
            <TextBlock Text="特别致谢" Style="{StaticResource GroupTitleStyle}"/>
            <Border Style="{StaticResource SettingRowStyle}">
                <StackPanel>
                    <TextBlock Style="{StaticResource ItemDescStyle}" FontSize="13" LineHeight="22"
                               Text="感谢「atao鱼」提供的优化版 Flash 插件。"/>
                    <TextBlock Style="{StaticResource ItemDescStyle}" FontSize="13" LineHeight="22"
                               Margin="0,6,0,0"
                               Text="以及「第一章」「fish229」「退后」「Ereshkigal」「丶丶丶」「豫风」「虎虎生威e」「墨简辰」「Cupid」「刘嘉欣」「热心市民刘大爷」「烟锁池塘柳」及一位无名氏参与调研。"/>
                </StackPanel>
            </Border>

            <!-- ===== 开源仓库 ===== -->
            <TextBlock Text="开源仓库" Style="{StaticResource GroupTitleStyle}"/>

            <!-- 游戏内核 -->
            <Border Style="{StaticResource SettingRowStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Style="{StaticResource SettingIconStyle}">
                        <TextBlock Text="&#xE774;" Style="{StaticResource SettingIconTextStyle}"/>
                    </Border>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="游戏内核" Style="{StaticResource ItemNameStyle}"/>
                        <TextBlock Style="{StaticResource ItemDescStyle}" Text="CEFFlashGameHost · 通用 CEF 87 Flash 渲染内核"/>
                    </StackPanel>
                    <ui:Button Grid.Column="2" Content="打开" Appearance="Secondary"
                               Click="OnOpenGameKernel" Margin="14,0,0,0" VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <!-- 启动器 -->
            <Border Style="{StaticResource SettingRowStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Style="{StaticResource SettingIconStyle}">
                        <Image Source="/assets/favicon.png" Stretch="Uniform"
                               Width="18" Height="18"
                               RenderOptions.BitmapScalingMode="HighQuality"/>
                    </Border>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="启动器" Style="{StaticResource ItemNameStyle}"/>
                        <TextBlock Style="{StaticResource ItemDescStyle}" Text="NarutoLauncher · 本启动器源码"/>
                    </StackPanel>
                    <ui:Button Grid.Column="2" Content="打开" Appearance="Secondary"
                               Click="OnOpenRepo" Margin="14,0,0,0" VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <!-- ===== 版权 ===== -->
            <TextBlock Style="{StaticResource ItemDescStyle}" HorizontalAlignment="Center"
                       TextAlignment="Center" Margin="0,18,0,0"
                       Text="本项目以 GNU AGPL-3.0 开源，版权所有 © 2026 XiaoWu。"/>

        </StackPanel>
    </ScrollViewer>
</UserControl>
