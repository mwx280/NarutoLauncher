<UserControl x:Class="NarutoLauncher.Views.AccountsView" x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:conv="clr-namespace:NarutoLauncher.Converters">
    <UserControl.Resources>
        <conv:QqAvatarConverter x:Key="QqAvatarConverter"/>
        <conv:AvatarCharDisplayConverter x:Key="AvatarCharDisplayConverter"/>
    </UserControl.Resources>
    <Grid Margin="24" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题行 + 头像类型 + 添加账号按钮 -->
        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="账号管理" FontSize="22" FontWeight="Bold"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                       VerticalAlignment="Center"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <ComboBox x:Name="AvatarDisplayBox" Width="120" Height="34" Margin="0,0,14,0"
                          Padding="10,0" VerticalContentAlignment="Center"
                          Background="{DynamicResource ControlFillColorDefaultBrush}"
                          BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                          BorderThickness="1" FontSize="13"
                          SelectionChanged="OnAvatarDisplayChanged">
                    <ComboBoxItem Content="备注首字"/>
                    <ComboBoxItem Content="账号首数字"/>
                    <ComboBoxItem Content="QQ 头像"/>
                </ComboBox>
                <ui:Button Content="添加账号" Appearance="Primary"
                           Width="110" Height="34" Click="OnAddAccount"/>
            </StackPanel>
        </Grid>
        <TextBlock Grid.Row="1" x:Name="CountText"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   FontSize="12" Margin="0,0,0,12"/>

        <!-- 账号列表 -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="AccountList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1" CornerRadius="8"
                                Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                                Padding="16,12" Margin="0,0,0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Width="42" Height="42" CornerRadius="21"
                                        Background="{DynamicResource AccentFillColorDefaultBrush}"
                                        Margin="0,0,12,0" ClipToBounds="True">
                                    <Grid>
                                        <TextBlock Foreground="{DynamicResource TextOnAccentFillColorPrimaryBrush}"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                                   FontSize="16" FontWeight="Bold">
                                            <TextBlock.Text>
                                                <MultiBinding Converter="{StaticResource AvatarCharDisplayConverter}">
                                                    <Binding/>
                                                    <Binding Path="AvatarDisplay"
                                                             RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                                </MultiBinding>
                                            </TextBlock.Text>
                                        </TextBlock>
                                        <Image Stretch="UniformToFill">
                                            <Image.Clip>
                                                <EllipseGeometry Center="21,21" RadiusX="21" RadiusY="21"/>
                                            </Image.Clip>
                                            <Image.Source>
                                                <MultiBinding Converter="{StaticResource QqAvatarConverter}">
                                                    <Binding/>
                                                    <Binding Path="AvatarDisplay"
                                                             RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                                </MultiBinding>
                                            </Image.Source>
                                        </Image>
                                    </Grid>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold"
                                                   Foreground="{DynamicResource TextFillColorPrimaryBrush}" FontSize="14"/>
                                        <Border Background="{DynamicResource ControlFillColorSecondaryBrush}"
                                                CornerRadius="4" Padding="5,1" Margin="8,0,0,0"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="{Binding LoginTypeText}"
                                                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                                       FontSize="11"/>
                                        </Border>
                                    </StackPanel>
                                    <TextBlock Text="{Binding InfoText}"
                                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                               FontSize="12" Margin="0,2,0,0"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2" Orientation="Horizontal"
                                            VerticalAlignment="Center">
                                    <ui:Button Content="切换区服" Appearance="Secondary"
                                               Width="80" Height="30" Margin="0,0,6,0"
                                               ToolTip="打开官网选区页，在游戏中自行选择区服"
                                               Click="OnChangeServer" Tag="{Binding Id}"/>
                                    <ui:Button Content="开始游戏" Appearance="Primary"
                                               Width="80" Height="30" Margin="0,0,6,0"
                                               Click="OnStartGame" Tag="{Binding Id}"/>
                                    <ui:Button Content="编辑" Appearance="Secondary"
                                               Width="70" Height="30" Margin="0,0,6,0"
                                               Click="OnEditAccount" Tag="{Binding Id}"/>
                                    <ui:Button Content="删除" Appearance="Danger"
                                               Width="70" Height="30" Margin="0,0,6,0"
                                               Click="OnDeleteAccount" Tag="{Binding Id}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
